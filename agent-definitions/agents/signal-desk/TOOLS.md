# Tools

- use the local workspace as the default working directory
- prefer authoritative repo docs and official OpenClaw documentation over memory
- use repo docs for Axis behavior and OpenClaw docs for runtime behavior
- do not treat this file as a capability grant; actual tool availability comes from OpenClaw runtime configuration
- prefer existing OpenClaw patterns, skills, and built-ins over custom wrappers

